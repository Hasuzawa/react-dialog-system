type Url = string

export interface StyledComponent {
	className?: string
	children?: React.ReactNode
}

type RGBColor = ``
type HexColor = ``
type sRGBColor = ``

type Color = RGBColor | HexColor | sRGBColor
